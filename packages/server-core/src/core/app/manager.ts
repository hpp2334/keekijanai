import { platformManager } from '../platform';
import { serviceManager } from '../service';
import { router } from '../controller';

export const app = {
  platformManager,
  serviceManager,
  router,
};
